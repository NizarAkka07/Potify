spring:
  datasource:
    url: jdbc:h2:mem:potify_payment_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

stripe:
  api:
    key: sk_test_mock_key
  frontend-url: http://localhost:9000

paypal:
  client-id: test_client_id
  client-secret: test_client_secret
  mode: sandbox
  api-url: https://api-m.sandbox.paypal.com

application:
  security:
    jwt:
      secret-key: test-only-jwt-secret-key-that-is-long-enough-1234567890
      expiration: 86400000
