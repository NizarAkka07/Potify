spring:
  datasource:
    url: jdbc:h2:mem:potify_user_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  mail:
    host: localhost
    port: 25


application:
  security:
    jwt:
      secret-key: test-only-jwt-secret-key-that-is-long-enough-1234567890
      expiration: 86400000
      refresh-token:
        expiration: 604800000
